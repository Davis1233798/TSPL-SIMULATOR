================================================================================
                    快速推送到 GitHub 指南
================================================================================

當前狀態:
✅ 代碼已 commit (2 個 commits 等待推送)
❌ 需要認證才能推送到 GitHub

錯誤原因:
當前 Git 使用了 JackChen6203 的認證,但倉庫屬於 Davis1233798

================================================================================
                    方法 1: 使用 Personal Access Token (推薦)
================================================================================

步驟 1: 創建 GitHub Personal Access Token
-------------------------------------------
1. 登入 GitHub 使用 Davis1233798 賬號
2. 進入: Settings → Developer settings → Personal access tokens → Tokens (classic)
3. 點擊 "Generate new token (classic)"
4. 配置:
   - Note: TSPL-SIMULATOR-Deploy
   - Expiration: 90 days 或 No expiration
   - Scopes: 勾選 repo (完整權限)
5. 生成並複製 token (格式: ghp_xxxxxxxxxxxx...)

步驟 2: 配置並推送
------------------
在 PowerShell 中執行:

# 設置遠端 URL (替換 YOUR_TOKEN 為剛才複製的 token)
git remote set-url origin https://Davis1233798:YOUR_TOKEN@github.com/Davis1233798/TSPL-SIMULATOR.git

# 推送
git push -u origin master

完成! ✅

================================================================================
                    方法 2: 使用 SSH 金鑰
================================================================================

步驟 1: 生成 SSH 金鑰
---------------------
# 生成金鑰
ssh-keygen -t ed25519 -C "your_email@example.com"
# 保存位置: C:\Users\solidityDeveloper\.ssh\id_ed25519
# 可以直接按 Enter 不設密碼

# 啟動 ssh-agent
Start-Service ssh-agent

# 添加私鑰
ssh-add C:\Users\solidityDeveloper\.ssh\id_ed25519

步驟 2: 添加公鑰到 GitHub
--------------------------
# 顯示公鑰
Get-Content C:\Users\solidityDeveloper\.ssh\id_ed25519.pub

1. 複製公鑰
2. 登入 GitHub (Davis1233798)
3. Settings → SSH and GPG keys → New SSH key
4. Title: TSPL-Dev-Machine
5. 貼上公鑰並保存

步驟 3: 推送
------------
# 更改遠端 URL 為 SSH
git remote set-url origin git@github.com:Davis1233798/TSPL-SIMULATOR.git

# 測試連接
ssh -T git@github.com

# 推送
git push -u origin master

完成! ✅

================================================================================
                    推送成功後的下一步
================================================================================

1. 訪問倉庫確認:
   https://github.com/Davis1233798/TSPL-SIMULATOR

2. 配置 GitHub Secrets (啟用自動部署):
   Settings → Secrets and variables → Actions → New repository secret

   需要添加:
   - DEPLOY_HOST: Ubuntu 主機 IP
   - DEPLOY_USER: SSH 用戶名
   - DEPLOY_SSH_KEY: SSH 私鑰完整內容
   - DEPLOY_PORT: 22

3. 查看 CI/CD 運行:
   Actions 標籤 → 查看 workflow 執行狀態

================================================================================
                    詳細文檔參考
================================================================================

- GIT_PUSH_GUIDE.md        - 完整推送指南
- DEPLOYMENT_SUMMARY.md    - 部署總結
- CICD_DEPLOYMENT.md       - CI/CD 詳細配置
- QUICK_START.md           - 快速開始
- TESTING_GUIDE.md         - 測試指南

================================================================================
                    常見問題
================================================================================

Q: Token 放哪裡最安全?
A: 使用 Git Credential Manager:
   git config --global credential.helper wincred
   推送時會彈出窗口輸入 token,會自動保存

Q: SSH 連接失敗?
A: 確認 ssh-agent 運行:
   Get-Service ssh-agent
   Start-Service ssh-agent
   ssh-add ~/.ssh/id_ed25519

Q: 推送被拒絕?
A: 先拉取遠端更改:
   git pull origin master --rebase
   git push -u origin master

================================================================================
                    立即開始! 選擇一種方法並執行
================================================================================

推薦: 方法 1 (Token) - 最簡單快速
備選: 方法 2 (SSH) - 更安全長期使用

祝部署順利! 🚀
